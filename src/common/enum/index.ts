export enum UserRole {
  ADMIN = 'admin',
  USER = 'user',
  SUPPER_ADMIN = 'supperadmin',
}

export enum BarberRole {
  BARBER_SHOP = "barber_shop",
  BARBER = "barber"
}

export enum Role {
  ADMIN = 'admin',
  BARBER = 'barber',
  USER = 'user',
}
